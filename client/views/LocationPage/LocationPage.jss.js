const locationPageStyles = theme => ({
    pageWrapper: {
        position: 'absolute',
        width: '100%',
        height: '100%',
        background: "#bfbfc0"
    }
});

export default locationPageStyles;